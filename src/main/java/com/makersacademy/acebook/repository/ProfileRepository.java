package com.makersacademy.acebook.repository;

import com.makersacademy.acebook.model.Profile;

import java.util.List;

public class ProfileRepository {
    public List findAll() {
        List profileList = new List; return profileList;
    }

    public Profile save(Profile newProfile) {
//        do some db stuff and return the profile
        Profile esther = new Profile();
        return esther;
    }

    public Profile findById(Long id) {
//        do some db stuff and return a list of matching profiles
        Profile ollie = new Profile();
        return ollie;
    }
}
